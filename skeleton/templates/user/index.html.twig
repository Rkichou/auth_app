{% extends 'base.html.twig' %}
{% block title %}Liste des utilisateurs{% endblock %}
{% block body %}
<h1>Utilisateurs</h1>
<div id="user-list">
    {% include 'user/_list.html.twig' %}
</div>
<a href="{{ path('user_new') }}" hx-get="{{ path('user_new') }}" hx-target="#modal-body" hx-trigger="click" hx-swap="innerHTML" class="mt-4 mb-6 inline-block px-4 py-2 bg-green-600 text-white rounded shadow hover:bg-green-700 transition">Créer un utilisateur</a>
<div id="modal-body" class="fixed inset-0 flex items-center justify-center z-50 bg-black bg-opacity-40 hidden"></div>
{% endblock %}
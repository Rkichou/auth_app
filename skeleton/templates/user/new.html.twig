{% extends 'base.html.twig' %}
{% block body %}
<h2 class="text-2xl font-bold mb-4">Créer un utilisateur</h2>
{{ form_start(form, {'attr': {'hx-post': path('user_new'), 'hx-target': '#user-list', 'hx-swap': 'innerHTML', 'class': 'space-y-4'}}) }}
    <div class="mb-4">{{ form_row(form.name, {'attr': {'class': 'block w-full px-4 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-400'}}) }}</div>
    <div class="mb-4">{{ form_row(form.email, {'attr': {'class': 'block w-full px-4 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-400'}}) }}</div>
    <button type="submit" class="px-4 py-2 bg-green-600 text-white rounded hover:bg-green-700 transition">Créer</button>
{{ form_end(form) }}
{% endblock %}